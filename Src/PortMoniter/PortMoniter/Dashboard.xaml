<Window x:Class="PortMoniter.Dashboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PortSniffer"
        mc:Ignorable="d"
        Name="home"
        Title="MainWindow" Height="700" Width="1180" MinWidth="1050" MinHeight="650"
        WindowStartupLocation="CenterScreen" WindowStyle="None"
        Background="Transparent" AllowsTransparency="True"
        ResizeMode="NoResize" WindowState="Maximized" MouseDown="Window_MouseDown"
        Icon="Assets/Images/icon.png">
    <Border CornerRadius="8" Background="{DynamicResource Backdrop}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <!-- Start: GridColum Left-->
            <Border CornerRadius="8 0 0 8" BorderBrush="{DynamicResource Flat3DBorderBrush}" Background="{DynamicResource SidePanel}">
                <Border.Effect>
                    <DropShadowEffect Direction="0" ShadowDepth="2" Opacity="1" BlurRadius="10" Color="Black" />
                </Border.Effect>
                <Grid x:Name="gridMenu" Width="63">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="60"/>
                        <RowDefinition Height="80"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="111*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>

                    <!-- Logo -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Center" Height="30">
                       
                    </StackPanel>

                    
                    <StackPanel Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <RadioButton GroupName="homeMenu" x:Name="sinffer"  Style="{DynamicResource MenuRadioButtonStyle}" Tag="{DynamicResource home}" Click="SnifferClick"/>
                    </StackPanel>

                    <StackPanel Grid.Row="3">
                        <RadioButton GroupName="homeMenu" x:Name="serialComm"  Style="{DynamicResource MenuRadioButtonStyle}" Tag="{DynamicResource notes}" Margin="0 5 0 5" Click="SerialCommClick"/>
                    </StackPanel>

                   
                    <!--<StackPanel Grid.Row="3">
                        <RadioButton x:Name="sinffer" IsChecked="True" Content="Port Monitor" Style="{DynamicResource MenuRadioButtonStyle}" Tag="{DynamicResource payment}" Click="rdHome_Click"/>
                        <RadioButton x:Name="serialComm" Content="Send" Style="{DynamicResource MenuRadioButtonStyle}" Tag="{DynamicResource notes}" Margin="0 5 0 5" Click="rdSounds_Click"/>
                    </StackPanel>-->

                </Grid>
            </Border>
            <!-- End: GridColum Left-->

            <!-- Start: GridColumn Rigth -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="43"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Start: Button Close | Restore | Minimize -->
                <StackPanel Orientation="Horizontal" Background="{DynamicResource Backdrop}" Panel.ZIndex="1" FlowDirection="RightToLeft"  Margin="0,0,0,10">
                    <Button  x:Name="btnClose" Style="{DynamicResource IconButtonsStyle}" Content="{DynamicResource close}" Click="btnClose_Click" Tag="IsCloseButton"/>
                    <Button  x:Name="btnRestore" Margin="5,0" Click="btnRestore_Click">
                        <Button.Style>
                            <Style TargetType="{x:Type Button}" BasedOn="{StaticResource IconButtonsStyle}">
                                <Setter Property="Content" Value="{DynamicResource maximize}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ElementName=home, Path=WindowState}" Value="Maximized">
                                        <Setter Property="Content" Value="{DynamicResource restore}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button   x:Name="btnMinimize" Style="{DynamicResource IconButtonsStyle}" Content="{DynamicResource minimize}" Click="btnMinimize_Click"/>
                    <ToggleButton Style="{DynamicResource TheameMode}" IsChecked="True" Margin="1" Checked="ToggleButton_OnChecked" Unchecked="ToggleButton_OnUnchecked" >
                     
                    </ToggleButton>
                </StackPanel>
                <!-- End: Button Close | Restore | Minimize -->

                <Border Grid.Row="1" Margin="0,-10,0,0">
                    <Frame x:Name="PagesNavigation" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"  NavigationUIVisibility="Hidden">
                        <!-- Page Content -->
                    </Frame>
                </Border>

                <!--<Frame Margin="0,-10,0,10" Grid.Row="1" VerticalContentAlignment="Center" HorizontalContentAlignment="Stretch" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"  x:Name="PagesNavigation"  NavigationUIVisibility="Hidden">
                        --><!-- Page Content --><!--
                    </Frame>-->
                
            </Grid>
            <!-- Start: GridColumn Rigth -->
        </Grid>
    </Border>
</Window>

<UserControl x:Class="PortMoniter.Pages.PartialViews.WriteDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
      
        <GroupBox Header="WRITE DATA" Style="{DynamicResource GroupBoxStyleOverride}">
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition  Height="*"/>
                    <RowDefinition  Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBox Grid.Row="0" Grid.Column="0" Foreground="{DynamicResource TextPrimaryColor}" Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" Margin="5 5 0 5" FontFamily="Consolas"
                         VerticalContentAlignment="Center">
                    <TextBox.InputBindings>
                        <KeyBinding Command="{Binding Send}" Key="Return"/>
                    </TextBox.InputBindings>
                </TextBox>
                <Button Grid.Row="0" Width="110" Height="30" Style="{DynamicResource btnBlue}" Grid.Column="1" Command="{Binding Send}" Content="Send" Margin="5" Padding="10 3">
                    <Button.Effect>
                        <DropShadowEffect ShadowDepth="0" BlurRadius="1"/>
                    </Button.Effect>
                </Button>

                <CheckBox Style="{DynamicResource SerialPortCheckbox}" Grid.Row="1" Grid.Column="0" x:Name="Autoscroll" VerticalAlignment="Center" VerticalContentAlignment="Center" IsChecked="{Binding IsHex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Content="Hex String"  />

            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
